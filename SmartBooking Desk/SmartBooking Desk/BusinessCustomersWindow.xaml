<Window x:Class="SmartBooking_Desk.BusinessCustomersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Klijenti"
        Height="650"
        Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#FFF7F7F7">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="0,0,0,14"
                BorderBrush="#22000000"
                BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="Klijenti"
                           FontSize="24"
                           FontWeight="SemiBold"/>
                <TextBlock Text="Pregled, pretraga i osnovna izmena klijenata za izabrani biznis."
                           Margin="0,6,0,0"
                           Foreground="#666"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1"
              Margin="0,16,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TextBoxSearch"
                     Grid.Column="0"
                     Height="34"
                     Margin="0,0,10,0"
                     VerticalContentAlignment="Center"
                     TextChanged="TextBoxSearch_TextChanged"
                     ToolTip="Pretraga po imenu, telefonu ili emailu."/>

            <Button x:Name="ButtonRefresh"
                    Grid.Column="1"
                    Content="Osveži"
                    Width="95"
                    Height="34"
                    Margin="0,0,10,0"
                    Click="ButtonRefresh_Click"/>

            <Button x:Name="ButtonNew"
                    Grid.Column="2"
                    Content="Novi klijent"
                    Width="120"
                    Height="34"
                    Margin="0,0,10,0"
                    Click="ButtonNew_Click"/>

            <Button x:Name="ButtonEdit"
                    Grid.Column="3"
                    Content="Izmeni"
                    Width="95"
                    Height="34"
                    Click="ButtonEdit_Click"/>
        </Grid>

        <DataGrid x:Name="DataGridCustomers"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="DataGridCustomers_SelectionChanged"
                  MouseDoubleClick="DataGridCustomers_MouseDoubleClick"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ime i prezime"
                                    Binding="{Binding FullName}"
                                    Width="2*"/>

                <DataGridTextColumn Header="Telefon"
                                    Binding="{Binding Phone}"
                                    Width="*"/>

                <DataGridTextColumn Header="Email"
                                    Binding="{Binding Email}"
                                    Width="1.5*"/>

                <DataGridTextColumn Header="Napomena"
                                    Binding="{Binding Notes}"
                                    Width="2*"/>

                <DataGridCheckBoxColumn Header="Aktivan"
                                        Binding="{Binding IsActive}"
                                        Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3"
              Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TextBlockStatus"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="#335"
                       Text="Učitavanje..."/>

            <Button x:Name="ButtonClose"
                    Grid.Column="1"
                    Content="Zatvori"
                    Width="110"
                    Height="36"
                    Click="ButtonClose_Click"/>
        </Grid>
    </Grid>
</Window>